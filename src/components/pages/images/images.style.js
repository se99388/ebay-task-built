import styled from 'styled-components';

export const Container = styled.div`
	.search-box {
		margin-bottom: 20px;
	}

	.images {
		display: flex;
		flex-wrap: wrap;
		.mars-image {
			margin: 10px;
		}
	}

	.pagination-box {
		display: flex;
		justify-content: center;
		margin-top: 30px;
	}
`;
