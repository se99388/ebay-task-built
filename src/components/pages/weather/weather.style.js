import styled from 'styled-components';

export const Container = styled.div`
	.search-box {
		margin-bottom: 20px;
	}

	.cards {
		display: flex;
		flex-wrap: wrap;
	}

	.pagination-box {
		display: flex;
		justify-content: center;
		margin-top: 30px;
	}
`;
